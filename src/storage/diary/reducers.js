const diariesReducer = ( state, action ) => {

    switch ( action.type ) {

        default: {
            throw new Error();
        }
    }
}

const diaryReducer = ( state, action ) => {

    switch ( action.type ) {

        default: {
            throw new Error();
        }
    }
}

export { diariesReducer, diaryReducer };
